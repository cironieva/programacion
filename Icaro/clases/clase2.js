//                                          CREAR FUNCIONES

// function <nombre> (parámetros) {return}
function sumarDosNumeros(parametro1, parametro2) {
    return parametro1 + parametro2;
}

// Definir una variable que almacene el return de la funcion, dar dos valores a los parámetros
let resultado = sumarDosNumeros(1, 2);


// Mostrar la variable que almacenó el return
console.log(resultado)

